import {describe,expect,test} from '@jest/globals';

describe('navigationService', () => {
    describe('matchRoute', () => {
        test('should pass', () => {
            expect(true).toBe(true);
        });
    });
});